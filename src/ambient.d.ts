declare type UserInfo = {
    username: String,
    gender: String,
    age: Number,
    country: String
}

declare type TodoTask = {
    id: Number,
    content: String,
    completed: Boolean,
    timestamp: Number
};